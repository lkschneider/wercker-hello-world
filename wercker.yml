box:
  id: openjdk
  username: $DOCKER_USERNAME
  password: $DOCKER_PASSWORD
  tag: 8

services:
    - mongo

build:
  steps:
    - script:
      name: Build
      code: 
        echo "Hello World!"

dev:
  steps:
    - script:
      name: Dev
      code: 
        echo "Hello Dev World!"
        echo "Hello Dev World again!"
    - lkschneider/wercker-hello-step@0.1.3 "https://github.com/lkschneider/wercker-hello-step/archive/master.tar.gz":
      myname: "linda"
    - script:
      name: export
      code:
        export;
        echo "Writing file to /pipeline/output/file.out";
        echo "OUTPUT" > /pipeline/output/file.out;
#    - internal/shell

cleanup:
  steps:
    - script:
      name: Cleanup
      code: 
        echo "Hello Cleanup World!"
